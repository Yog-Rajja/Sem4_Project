Django==5.1.5
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
python-dotenv==1.0.1
requests==2.32.3
# Pure-Python PDF text extraction. Scanned pages fall through to Gemini's
# multimodal reading instead, so there is no OCR engine to install.
pypdf==5.1.0
